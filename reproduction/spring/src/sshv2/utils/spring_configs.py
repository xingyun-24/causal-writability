"""Typed configuration for the standalone spring experiment."""

from sshv2.diffsynth.utils.configs import TrainingConfig


class SpringTrainingConfig(TrainingConfig):
    """Training configuration for the spring shortcut experiment only."""

    pass
